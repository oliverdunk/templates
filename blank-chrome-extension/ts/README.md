A simple Chrome Extension starter template

## Getting Started

Build the project with `npm run build`.

To test your extension locally:

1. Run `npm run zip`.
2. Right click and download `extension.zip` from the file tree.
3. Navigate to chrome://extensions in the address bar.
4. Enable "Developer Mode" in the top right.
5. Use the "Load Unpacked" option to load the downloaded file.
